version: "3.9"

services:
  postgres:
    image: postgres:16
    container_name: minly-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: minly
      POSTGRES_PASSWORD: minly_password
      POSTGRES_DB: minly_db
    ports:
      - "5432:5432"
